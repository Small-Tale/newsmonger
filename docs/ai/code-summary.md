# Codebase Map (AI summary)

> Read this + `requirements-summary.md` at the start of a fresh session. Keep both in sync with reality in the same change as the code.
>
> **This file is hand-written.** Its neighbour's *status block* is not: `npm run docs:requirements` regenerates it from the FR docs' own markers (NEWS-296), and its notes region stays hand-written. Only status was mechanical enough to generate; a generated "where do I look for X" index would be a directory listing.

**Newsmonger**: topic-based news tracker. Node/TS server (Hono) + kerfjs client; Claude (web search) finds news per topic on a schedule, deduplicated against previously seen stories. JSON-file storage. Optional Tauri desktop shell (dev-mode only so far).

## Directory tree

```
src/
  cli.ts              entry: arg parse → Store/service/runner → server → scheduler → open browser
  config.ts           CLI flags (--port --data-dir --provider --model --endpoint --no-open --strict-port --ai-test --demo) + env; USAGE_LINE/HELP_TEXT + earlyExitFlag (--help/--version, scanned before parseArgs — NEWS-216)
  version.ts          appVersion(): nearest package.json version, cached, '' if not found — used by /api/state and --version
  server.ts           createApp (Hono, DI via middleware) + startServer (127.0.0.1, port fallback), /static handler
  export.ts           toMarkdown/toJson/toAtom + escapeXml — export & feed rendering, pure (NEWS-85)
  origin-guard.ts     Host/Origin check on every route — cross-origin + DNS-rebinding guard (NEWS-86)
  discovery.ts        DiscoveryService: topic-suggestion exclusions + in-memory cache keyed on request *and* provider|model|effort (NEWS-125, NEWS-258) + classification validation + call log
  backup-locations.ts suggestedBackupLocations (probes iCloud/Drive/OneDrive/Dropbox per OS, incl. the macOS CloudStorage prefix scan; NEWS-230) + normalizeBackupDir (expands ~, refuses relative — node-only, so NOT in api/schemas.ts which the client imports; NEWS-237)
  backup.ts           Backups/writeBackup/buildBackup — snapshot to the user's backupDir in DataFileSchema shape, temp+rename, 1/hour (NEWS-192)
  undo.ts             ClearUndoBuffer: in-memory, per-topic, TTL'd snapshot of a cleared topic's stories + covered window (NEWS-145)
  threads.ts          story threads (NEWS-280): planThreadIds/threadIdFor/withThreadIds — pure local similarity (content tokens minus the topic's own words, capitalized entities, shared host as tie-break only, 30-day recency). **Not dedup** — see docs/29-story-threads.md
  scheduler.ts        startScheduler: 60s tick + 3s startup sweep, non-overlapping; drains an overrun cycle (NEWS-57)
  checks.ts           CheckRunner (checkTopic/checkDue/checkAll, in-flight guard) + cancelStaleChecks() on a settings change (NEWS-257) / cancelAllChecks() on a story clear (NEWS-271, also stops the sweep queue via cancelEpoch and discards results arriving post-abort) + isDue()/isDueDaily()/isDueUnderSchedule()/lastSlotBefore() (NEWS-84) + effectiveInterval() + byCheckOrder() (most-overdue-first, NEWS-58). No budget logic — NEWS-119 removed it
  types.ts            Hono AppEnv (store, runner injected)
  keychain.ts         OS credential store via platform CLI (security/secret-tool/cmdkey)
  images/             og:image scrape + favicon resolution + local cache; safety.ts holds the SSRF guards
    favicon.ts        originOf (cache key) + extractIconUrl + faviconCandidates (NEWS-169)
  attendance.ts       Attendance: in-memory lastSeenAt + 5 min window; gates attended providers
  db/
    schemas.ts        zod: Topic, NewsItem, Settings, CheckRun, DataFile; DEFAULT_CHECK_INTERVAL_MS, MAX_GUIDANCE_LENGTH
    store.ts          Store: SQLite (node:sqlite), per-row writes, zod-validated rows, corrupt-db backup+reset, one-time data.json import (NEWS-94)
    sqlite.ts         schema DDL as **TABLES then MIGRATIONS then INDEXES** (an index on a column a migration adds cannot be created before it — NEWS-280), SCHEMA_VERSION + MIGRATIONS (user_version based), openDb (WAL + sanity probe), backupUnreadableDb, dbPath
    warnings.ts       filters ONLY node:sqlite's ExperimentalWarning; imported before the require in sqlite.ts
  ai/
    types.ts          AUTO_ORDER (client-safe, NEWS-128); DISCOVERY_MODELS + usesLegacyRequestShape (NEWS-132); NewsService (checkTopic + suggestTopics) + NewsProvider, TopicContext, CheckResult/TokenUsage, SuggestRequest/SuggestScope/TopicSuggestion (NEWS-124), PROVIDER_NAMES/INFO, FoundNewsItem, KnownItem
    prompt.ts         searchingSystemPrompt, buildUserPrompt, parseNewsResult, NEWS_JSON_SCHEMA
    suggest-prompt.ts topic *discovery* prompting: suggestSystemPrompt, buildSuggestPrompt, parseSuggestResult, SUGGEST_JSON_SCHEMA (NEWS-124)
    retry.ts          two backoffs (in-check DEFAULT_BACKOFF, per-topic FAILURE_COOLDOWN), failure classification, Retry-After parsing (NEWS-109/110)
    verify-links.ts   probeLink/verifyItemLinks — citation checking before storage (NEWS-83)
    verify-key.ts     verifyApiKey — vendor-side key check before saving; 401/403 = invalid, else unknown (NEWS-78)
    api-keys.ts       resolveApiKey/saveApiKey/deleteApiKey — env then keychain, never the database
    sanitize.ts       stripMarkup — strips model citation markup (<cite>) from prose, idempotent
    dedupe.ts         normalizeUrl/normalizeTitle/dedupeKeyFor/filterNewItems
    providers/
      index.ts        PROVIDERS/FACTORIES, AUTO_ORDER, resolveProvider, unavailableMessage
      anthropic.ts    createAnthropicProvider (opus-4-8, adaptive thinking, web_search_20260209, streamed); messageParams() builds the body — shape varies by model for the haiku discovery path (NEWS-132)
      claude-cli.ts   createClaudeCliProvider — Claude subscription via the Claude Code CLI; attended: true
      codex-cli.ts    createCodexCliProvider — ChatGPT subscription via Codex (-s read-only); attended: true
      openai.ts       createOpenAIProvider (Responses API + hosted web_search, output_text); OPENAI_BASE_URL
      mock.ts         createMockProvider (--ai-test; deterministic; "fail"→throws, "empty"→[], "thread"→one subject's series, two more instalments per check so a thread can outgrow THREAD_ROW_CAP); suggestTopics keys off a request seed and plants an excluded name on purpose (NEWS-124)
  api/
    schemas.ts        zod request schemas + StateResp (shared client/server)
  routes/
    api.ts            /api/discover + /api/discover/usage (NEWS-125), /api/state (topics/settings/runs/checking + latestItemIds + flaggedByTopic + todayByTopic/newestItemAtByTopic for the sidebar badge + newest sort, NEWS-242/241; NO items), /api/items (paginated feed: filter+sort+cursor + `threads` = per-story thread shape, NEWS-282), /api/items/:id/thread (one story's whole thread, oldest first; 404 on an unknown id — NEWS-282), /api/providers, /api/topics, /api/items/:id (save/flag), /api/settings, /api/backup + /api/backup/locations (NEWS-192, NEWS-230), /api/keys, /api/foreground, /api/check, /api/open-external, /api/export.md, /api/export.json, /api/export-topics.json + /api/import-topics (the shareable topic list, NEWS-317/318), /api/import-stories (NEWS-319), /api/topics/clear (delete every topic, NEWS-328), /feed.xml, /healthz
    pages.tsx         GET / — SSR shell
  components/
    layout.tsx        HTML shell
  client/
    app.tsx           kerf UI: mount + **every `delegate()`**; header/banners/settings dialog/topics/onboarding + the top-level shell. Views are being split out by *view* (NEWS-297) — handlers stay here, because two delegates matching one gesture both run (NEWS-126)
    filter-bar.tsx    the section filter bar under the masthead — filters the *feed* by taxonomy, despite rendering above the rail (NEWS-297)
    dialogs.tsx       the dialogs whose markup owns no state: export, privacy, guidance, rename, the backup offer (NEWS-297). `confirmDialogJsx` is **not** here — it reads with `confirm()`, whose promise is the mechanism
    key-row.tsx       `keyRowJsx` — one provider's API-key row, shared by Settings → Source *and* onboarding's Source step (NEWS-297)
    onboarding-view.tsx the first-run wizard's markup (NEWS-297); the step machine stays in `onboarding.ts` + the store
    discover-view.tsx the topic-discovery dialog + tuner markup (NEWS-297); the state machine is `discover.ts`, the handlers stay in `app.tsx`
    topics-view.tsx   the Watching rail: `dialJsx`, `topicRowJsx`, and the topic context menu (NEWS-297). **Not** `filterBarJsx` (that filters the feed) nor `itemMenuJsx` (a story's menu — in `feed.tsx`)
    settings.tsx      the whole settings dialog — tabs, all four panels, the source status line (NEWS-297). Theme picker in App → Appearance (NEWS-334). One export, `settingsDialogJsx`. **No diagnostics or effort comparison since NEWS-333** — both sections removed on request, along with `diagnostics.ts` and `effort-stats.ts`
    feed.tsx          the feed: `itemJsx`/`flaggedRowJsx`/`feedJsx`, the day grouping, the thread pane and badge, and the story context menu (`itemMenuJsx`). Rendering only — first seam out of `app.tsx` (NEWS-297)
    stores.ts         appStore (defineStore)
    api.ts            fetch wrappers, refreshState (zod-validated), withRefresh
    icons.tsx         ALL icons live here — Lucide paths inlined (lucide-static@1.26.0), no runtime dep, no emoji
    tauri.ts          __TAURI__ detection, getTauriInvoke, openExternalUrl, bounceDockIcon, focusAppWindow (tests: tauri-window.test.ts, NEWS-261)
    notifications.ts  noteState — OS notification when new items arrive while unfocused (NEWS-38); sendTestNotification (NEWS-260). One path: `new Notification(...)` is the Tauri plugin on the desktop (its init script replaces `window.Notification`) — there is no `__TAURI__.notification` global
    share.ts          shareText + shareItem — OS share sheet, clipboard fallback (NEWS-43)
    schedule.ts       isBehindSchedule/topicsBehindSchedule — falling-behind detection for the banner (NEWS-59)
    failure.ts        currentFailure — the topic-currently-failing warning source (NEWS-41); dismissal persisted in localStorage
    search.ts         itemMatchesQuery/filterItemsByQuery — live feed search filter (NEWS-60)
    attribution.ts    outletFor/publishedLabel — source outlet + publication date display (NEWS-82)
    dates.ts          dayKeyOf/dayLabel/dayLabelFor — the app's ONE absolute date format (Today / Yesterday / "Jul 20"), shared by the feed's day headings and thread rows (NEWS-282) — plus `relativeTime`, its relative sibling, moved here in NEWS-297 so `app.tsx` and `feed.tsx` need not import each other
    thread-view.ts    THREAD_ROW_CAP/visibleThreadRows/threadRowDate/showAllLabel/threadFetchNeeded — the thread pane's reading rules (NEWS-282); threadBadge/threadBadgeLabel/threadExpanderLabel — the collapsed card's badge wording (NEWS-283)
    discover-progress.ts estimateTargetMs/recordDuration — the discovery progress-bar estimate, median of the last 10 durations in localStorage (NEWS-137)
    discover.ts       groupSuggestions/resultsHeading/sectionTiles (NEWS-126) + the pure tuner state machine: startTuner/judgeCandidate/nextRound/mergeKept (NEWS-127)
    solo.ts           toggleSolo/isAllSoloed — solo-set arithmetic shared by the context menu and the double-click gesture (NEWS-95)
    dial.ts           dialRemaining/dialCountdownMs — the sidebar ring's countdown fraction + tooltip (NEWS-144/202). Counts from `lastCheckedAt ?? clearedAt`, mirroring the server's `scheduleBaseline` — the ring is a claim about the *future*, so a cleared topic counts down even though the row's text says "not checked yet" (NEWS-291)
    export-url.ts     exportHref — the export dialog's scope × topic × format URL, null when the choice can't be exported (NEWS-158/160)
    menu-position.ts  placeMenu/menuStyle — clamps a context menu into the viewport so its last item stays reachable (NEWS-149)
    onboarding.ts     onboardingCountText — the Topics step's running count, which names ticked-but-not-created and already-created separately (NEWS-146)
    rail.ts           railTopPx/trackRailTop — publishes --rail-top from the rail's VIEWPORT top
                      (getBoundingClientRect, never offsetTop: it is sticky-shifted — NEWS-339)
    topic-sort.ts     sortTopics (NEWS-63) + topicRows — the sidebar's rows, with section headings interleaved in the By-section sort (NEWS-140)
    styles.scss       styling (light/dark via prefers-color-scheme)
src-tauri/            Tauri v2 shell; one spawn path, dev runs tsx + release runs the sidecar
  src/lib.rs          server_command() picks the command; spawn_server() watches stdout + navigates,
                      captures stderr, and startup_failure_detail() explains a server that died first
  loading/index.html  the pre-navigation page: spinner, and the failure state showExited/showError paint
  binaries/           gitignored: newsmonger-node-<triple> (real Node binary, externalBin sidecar)
  server/             gitignored: staged cli.js + client/ + node_modules (bundled as `resources`)
scripts/
  build-sidecar.sh    builds/stages the above, then boots it from a temp dir to verify
  e2e-preflight.mjs   clears the derived E2E ports before Playwright's own check: reclaims an orphaned server from a crashed run, refuses a live one with a sentence (NEWS-287)
  rust-changed.sh     "does the local change touch Rust?" — exit 0 = run the gates (NEWS-294)
  gate-quick.sh       the inner loop: typecheck + lint + unit, no coverage/Rust/E2E (NEWS-294)
  build-requirements-summary.mjs
                      regenerates requirements-summary.md's status block from the FR docs' markers; --check is the gate (NEWS-296)
  merge-coverage.mjs  merges unit + E2E coverage. **The unit run is the basis** — it alone remaps through source maps;
                      c8's LF is the file's whole line count (NEWS-357). Refuses a source with no unhit lines at all
  npm-command.mjs     npmSpawn/npxSpawn — Windows needs the `.cmd` name AND `shell: true`, together (NEWS-348/354/356)
.github/              CI: gate job (test:all) + rust job (fmt + clippy, BOTH profiles); dependabot
tests/
  helpers/            tmp.ts (tmp data dirs), provider.ts (asResolver/fakeProvider), e2e-port.ts (per-checkout E2E port window — NEWS-287)
  unit/               vitest: dedupe, store, checks, scheduler, config, parse-result, providers, openai, api, api-keys, api-keys-routes, attendance, catch-up, sanitize, origin-guard, guidance, key-verify, retention, export, daily-schedule, verify-links, attribution, concurrency, suggest-prompt, suggest-providers, discovery, discover-client, discover-progress, item-expand, requirements-summary; plus the **rule guards**, which assert about platforms/values the run itself cannot exercise — sandboxable (no tsx CLI, walks the tree), windows-portability (no bare npm/npx, no URL.pathname as a path), e2e-global-setup, color-contrast (palette vs WCAG, since axe only sees what renders), merge-coverage
  e2e/                playwright, **sharded by file** (NEWS-321: a server per worker from a worker-scoped fixture in `server.ts`, since Playwright's `webServer` is global; `globalSetup` builds the client once; `E2E_WORKERS=1` goes back to serial), mock AI (--ai-test), a five-port window derived per checkout (NEWS-287/321, `tests/helpers/e2e-port.ts` — pick a worker's port with `parallelIndex`, never `workerIndex`): app.spec.ts (shell, topics, the check loop, scheduling warnings), settings-source.spec.ts + settings-layout.spec.ts, stories.spec.ts (card, bookmark/share/expand/flag, search, grid), export.spec.ts, backup.spec.ts, notifications.spec.ts (+ the dismissable banners) — **the last six were one 2,484-line `app.spec.ts` until NEWS-322**, which was 38% of the suite's runtime and the floor on any sharded run; plus keys.spec.ts, topics.spec.ts, a11y.spec.ts (axe-core, both themes), categories.spec.ts (NEWS-97), discover.spec.ts (NEWS-126), threads.spec.ts (NEWS-280–293), backup-prompt.spec.ts, update.spec.ts, layout.spec.ts (full-window layout + column count at several viewports, NEWS-96). `seedCheckedTopic` in `fixtures.ts` is how a split file states the precondition it used to inherit from the tests above it (NEWS-322) — it waits for the stories the topic's automatic first check produces, and throws rather than returning early. `resetSharedState` in a beforeAll gives every attempt — first run or serial retry — the same precondition: no topics, the backup offer silenced, **every** setting back at its default and no stored API keys (NEWS-101, widened in NEWS-313 after a skipped cleanup in `discover.spec.ts` made `keys.spec.ts` fail in a different file minutes later, and widened again in NEWS-321 to all of them — sharding makes *which* files share a worker's server vary run to run, so a setting one file leaves changed is a different coupling every run rather than a stable one). **Every spec file calls it**, so no file's precondition depends on the previous file's cleanup running. **One browser context for the whole run** (worker-scoped `sharedContext`), pages still per-test: a context owns its connection pool, and a fresh one per test churned 457 sockets into `TIME_WAIT` — 46 now. `localStorage` and permissions are cleared in teardown to keep the isolation that gave (NEWS-246)
docs/                 numbered requirements (1–31), ai/ summaries, manual-test-plan.md
```

## Data schema (`<data-dir>/newsmonger.db`, SQLite — NEWS-94)

Tables `topics` / `items` / `runs` / `meta` (settings as one JSON row). Booleans are INTEGER 0/1; `sources`, `image` and `usage` are JSON columns. No foreign keys — a check can outlive its topic, so `deleteTopic` cascades explicitly. The shapes below are the zod schemas every row is validated against on read.

- `topics[]`: id, name, paused, highPriority (checked on the shorter interval — NEWS-56), guidance (free-text steer fed to the prompt — NEWS-80), createdAt, lastCheckedAt (every attempt; **what the UI displays**), coveredThroughAt (successes only — drives the prompt window), clearedAt (set by a clear; the **fallback due-ness baseline** so a reset topic reads as never checked without being due — NEWS-291)
- `items[]`: id, topicId, title, summary, sources[{title,url,outlet,publishedAt — NEWS-82}], image, saved (bookmark), offTopic (NEWS-61 flag), dedupeKey, threadId (NEWS-280 — the thread's first story's id; its **own** id = a thread of one), foundAt
- `settings`: itemRetentionDays (default 365, 0 = forever — NEWS-87), checkIntervalMs (default 1 day, min 5 min), highPriorityIntervalMs (≤ checkIntervalMs, clamped on update+load — NEWS-56), provider (default `auto`, `.catch('auto')` for retired providers), model (''), endpoint (''), notifyOnNewItems, monthlyBudgetUsd (0 = no cap — NEWS-79)
- `runs[]`: id, topicId, startedAt, finishedAt, status(running|succeeded|failed), newItems, error, provider, model, usage (tokens+searches, null = unknown — NEWS-79) (last 200)

Data dir: `--data-dir` flag → `NEWSMONGER_DATA_DIR` → `~/.newsmonger`. Also holds `newsmonger.db-wal`/`-shm`, `prices.json` (live model rates, user-editable — NEWS-93) and the image cache.

## Build / run / test

- `npm run dev` — build client (esbuild IIFE + sass, **dev bundle**) then run server from source (tsx); port 4187
- `npm run build:client` (prod, `__KERF_DEV__=false`) vs `build:client:dev` (kerf diagnostics + `invariants: 'throw'`) — NEWS-100
- `npm run build` — tsup → `dist/cli.js`; `npm run build:client` → `dist/client/`
- `npm run tauri:dev` — desktop dev shell (needs Rust; verified on macOS)
- `npm run tauri:build` — release app + dmg; runs `scripts/build-sidecar.sh` via `beforeBuildCommand`
- `npm test` (vitest+coverage) · `npm run test:e2e` (playwright) · `npm run test:all` (typecheck+lint+rust+unit+e2e)
- `npm run test:all -- --from=<phase>` (NEWS-300) — resume the gate at a phase instead of from the top; `--list` names them. Every run prints a per-phase summary; a resumed one is loudly **not** a commit gate. `scripts/gates-rust.sh` reports its own self-skip through `RUST_GATE_VERDICT_FILE`, so the summary cannot claim Rust ran when it did not
- `npm run gate:quick` (`scripts/gate-quick.sh`, NEWS-294) — the inner loop: typecheck + lint + client build + unit tests, **no coverage, no Rust, no E2E**. ~21 s vs ~5 min. It prints what it skipped and that `test:all` is still the commit requirement
- **The unit suite depends on both builds.** `scripts/test-all.sh` runs `build:client` (several tests fetch `/static/…` through the real route — NEWS-191) *and* `build` (`tests/unit/npm-package.test.ts` spawns `node dist/cli.js` — NEWS-295) before vitest. Handled differently on purpose: the CLI test rebuilds `dist/cli.js` itself when it is missing or older than `src/`, so a bare `npm test` works on a clean checkout, while `api.test.ts` fails with the command to run for `dist/client/`
- `npm run record:cli-sessions` — capture real CLI transcripts to `tests/fixtures/cli-sessions/` (NEWS-277). Replayed by `tests/unit/cli-session-replay.test.ts` through `spawnRunner(name, exec)`, the process-boundary seam in `src/ai/providers/cli-exec.ts`, so argv/schema/parsing/error-formatting all run for real offline. Two fixtures are deliberate failures
- `npm run test:e2e:real` — the **real** Claude/Codex subscriptions (NEWS-276), opt-in via `NEWSMONGER_E2E_REAL=1`, excluded from `test:all` by `testIgnore` in `playwright.config.ts`. Own server on the second port of this checkout's derived window (NEWS-287), no `--ai-test`. Guards the class of bug the mock cannot see: a vendor CLI flag disappearing, a schema strict mode rejects (NEWS-272), and the populated effort comparison the mock's single effort level makes unreachable (NEWS-227)
- `npm run docs:requirements` — regenerate `docs/ai/requirements-summary.md`'s status block from the FR docs (NEWS-296). Run it after adding or re-marking a requirement; `tests/unit/requirements-summary.test.ts` runs the same script with `--check` and fails the gate when the committed file is stale. The hand-written notes region is copied through byte for byte, so regenerating cannot lose anything
- `npm run commit:msg` — gitgist drafts a commit message from the staged diff. Output is Conventional Commits and must be reshaped to this project's style; see CLAUDE.md → Git
- Lint/typecheck: `npm run lint` / `npm run typecheck` (eslint strictTypeChecked + eslint-plugin-kerfjs)
- Rust: `cd src-tauri && cargo fmt --check && cargo clippy --all-targets -- -D warnings`, **and the same with `--release`** — the dev/release spawn paths are `cfg`-gated, so a debug-only check never compiles the release branch
- `npm run gates:rust` (`scripts/gates-rust.sh`) runs all four exactly as CI does, and **path-gates itself** (NEWS-294): `scripts/rust-changed.sh` exits 1 when nothing under `src-tauri/` nor a Rust-adjacent script differs from the upstream, and the gates skip with a loud notice. `RUST_GATES=required` forces them; `RUST_GATES=skip` suppresses them (ci.yml's gate job); `CI` being set disables the auto-skip entirely
- CI (`.github/workflows/ci.yml`) runs all of the above; **never executed — the repo has no remote yet** (NEWS-6)

## Where do I look for X?

| X | Look in |
|---|---|
| Add/modify an API endpoint | `src/routes/api.ts` + `src/api/schemas.ts` (+ client `src/client/api.ts`) |
| Add/change an AI provider | `src/ai/providers/` (+ register in `index.ts`); interface in `src/ai/types.ts` |
| Change prompts / result parsing | `src/ai/prompt.ts` |
| Provider selection / auto order | `src/ai/providers/index.ts` (`resolveProvider`, `AUTO_ORDER`) |
| Export dialog | `exportDialogJsx` in `client/dialogs.tsx` (NEWS-297) + the `open-export`/`close-export`/`data-export-scope`/`data-export-format` delegates in `client/app.tsx`, `export` in `client/stores.ts`, `.export-option` in `styles.scss`. Tauri routing via `data-export` → `/api/open-external`. See `docs/21-export-and-feed.md` FR-21.8/21.9 |
| API key auto-save | `commitKey` + the `submit`/`change` delegates in `client/app.tsx`, `savingKey` in `client/stores.ts`, `.key-saving` in `styles.scss`. See `docs/7-api-keys.md` FR-7.10a |
| Undoing a clear | `src/undo.ts` (`ClearUndoBuffer`), `Store.clearItemsForTopic`/`restoreClearedItems`, `POST /api/topics/:id/restore-cleared`, `showUndoToast` + `[data-undo-clear]` in `client/app.tsx`. **`ClearedItems` must carry every field the reset touches** — since NEWS-291 that is the whole check state plus the *previous* `clearedAt`, or an undo lands the topic in a state no real sequence could produce. See `docs/26-undo.md` |
| Sidebar today-count badge / newest sort | `store.itemStatsByTopic(startOfDayIso)` — one query for both, excludes off-topic, omits topics with none (absence is what makes the sort sink them). Surfaced on `/api/state`; badge in `topicRowJsx` **and in the `each()` memo key** (it lives outside the topic object); `'recent'` case in `src/client/topic-sort.ts`. See `docs/3-ui.md` FR-3.60/3.61 |
| Dedup behavior | `src/ai/dedupe.ts` (keys), `src/checks.ts` (application) |
| Story threads ("story so far") | `src/threads.ts` (pure similarity + `planThreadIds`), assigned in `CheckRunner.checkTopic` right before the insert, `Store.backfillThreads()` (startup, from `cli.ts`) + `Store.threadForItem(id)`; column `items.thread_id` (schema v5). **A different question from dedup** — URL identity vs subject identity. Mock keyword **"thread"** returns one subject's series, growing by two per check. See `docs/29-story-threads.md` |
| The thread timeline in an expanded card | `GET /api/items/:id/thread` (404 on unknown) + `Store.threadSummaries(items)` → `threads` on `/api/items`; `loadThread` in `client/api.ts` (fetch **on expand**, cached per story id + size; `refreshFeed` re-calls it for the open card each poll — NEWS-293 — where the size check makes an unchanged thread free, a refresh keeps the old rows and survives a failure, and `threadsInFlight` guards it because a refresh leaves the pane `ready`); `threads`/`threadPanes`/`threadShowAll` in `client/stores.ts`; `threadPaneJsx`/`threadRowJsx` + the `[data-retry-thread]` / `show-all-thread` delegates in `app.tsx`; rules in `client/thread-view.ts`; `.thread*` in `styles.scss`. A thread of one — the common case — renders one line, **no heading, no request**. See `docs/29-story-threads.md` FR-29.28–29.35 |
| The "4th update" badge on a collapsed card | `threadBadge`/`threadBadgeLabel`/`threadExpanderLabel` in `client/thread-view.ts`; rendered **inside the expander button** in `itemJsx` (`.thread-badge`), `.item-action.expand.threaded` in `styles.scss`. Data comes from the feed page's `threads` map — **never a fetch per card**. Card shows the count only; the date is in `title`/`aria-label` (a feed column is ~430px and the date clause reflowed the topic pill onto three lines — NEWS-71). See `docs/3-ui.md` FR-3.67 + `docs/29-story-threads.md` FR-29.36 |
| Dead / hallucinated source links | `src/ai/verify-links.ts`, called from `CheckRunner.verifyLinks` **before** dedup. Reuses `images/safety.ts` SSRF vetting; null probe under `--ai-test`. See `docs/2-news-checks-and-dedup.md` FR-2.14–2.18 |
| Scheduling rules | `src/checks.ts` (`isDueUnderSchedule` picks interval vs daily — NEWS-84; `isDue`, `effectiveInterval`, `byCheckOrder`, `scheduleBaseline`), `src/scheduler.ts` (tick + overrun drain). **Adding a topic checks it immediately** — `POST /api/topics` fires `checkTopic({manual:true})` in the background (NEWS-54, FR-1.12). **Due-ness reads `scheduleBaseline`, never `lastCheckedAt` directly** — a cleared topic has none (NEWS-291) |
| How many checks run at once | `checkConcurrency` setting + `CheckRunner.runPool` (shared cursor, `byCheckOrder` start order). See `docs/13-scheduling-under-load.md` FR-13.4–13.7 |
| Cancelling in-flight checks when settings change | `CheckRunner.cancelStaleChecks()` — per-check `provider\|model\|effort` signature + `AbortController`; `AbortSignal` reaches both SDKs and `SIGTERM`s the CLI children; cancelled runs are **deleted**, not failed; manual checks reissued after a 750 ms coalescing window, scheduled ones left due. Triggered from `PATCH /api/settings`. NEWS-257, FR-2.11/2.12 |
| Behaviour under load (overrun, ordering, falling-behind) | `src/scheduler.ts` drain (NEWS-57), `byCheckOrder` in `src/checks.ts` (NEWS-58), `src/client/schedule.ts` + the "falling behind" banner in `app.tsx` (NEWS-59). See `docs/13-scheduling-under-load.md` |
| "Falling behind" warning firing when nothing was wrong | `checksPossibleSince` on `/api/state` — `CheckRunner.checksPossibleSince()`, moved by the deferral in `checkDue` — read by `isBehindSchedule` in `src/client/schedule.ts`. Lateness runs from the later of `lastCheckedAt` and that watermark, so time backgrounded (attended providers) or rate-limited is not counted against the app. See `docs/13-scheduling-under-load.md` FR-13.3a |
| Topic priority (high-priority interval) | `highPriority` on the topic + `highPriorityIntervalMs` in settings; `effectiveInterval` in `src/checks.ts`; clamp in `store.updateSettings` + `SettingsSchema` transform; menu action `priority` + star icon in `app.tsx`. See `docs/12-topic-priority.md` |
| How far back a check asks | `coveredThroughAt` on the topic → `sinceIso` → `windowLine()` in `src/ai/prompt.ts`; **not** `lastCheckedAt` |
| How much a check returns | `searchingSystemPrompt()` volume rule (portable); `max_uses: 8` in `anthropic.ts` is a cost guard, not the mechanism |
| Persistence / schema change | `src/db/schemas.ts` + `src/db/store.ts` — **removing an enum value needs `.catch()`** or old files get reset (see the migration tests) |
| UI change | `src/client/app.tsx` (+ `styles.scss`) — **but the feed's cards, day grouping and thread pane are in `src/client/feed.tsx`, the export/privacy dialogs in `src/client/dialogs.tsx`, the key row in `key-row.tsx`, the first-run wizard in `onboarding-view.tsx`, the topics rail in `topics-view.tsx`, discovery in `discover-view.tsx` and **the entire settings dialog in `settings.tsx`** since NEWS-297, an ongoing split by *view*. `delegate()` registration stays in `app.tsx` whatever moves out. Mind the kerf structural rules in `docs/3-ui.md`. **kerfjs 4.1.0** — dev diagnostics are opt-in by import; we import them **only in the dev bundle**, behind the `__KERF_DEV__` esbuild define, with `invariants: 'throw'` (NEWS-100). KF-377 is fixed (NEWS-99), and enumerated attributes take keyword strings (NEWS-123). 4.1.0 changed nothing in the runtime — its `dist` is byte-identical to 4.0.0's; the release was the ESLint plugin declaring a tested peer range (`^9 || ^10`) instead of an open-ended `>=8` |
| New CLI flag | `src/config.ts` + `src/cli.ts` — and add it to `HELP_TEXT` in `config.ts`, which `tests/unit/config.test.ts` pins against what `parseArgs` accepts |
| Which model the picker shows / defaults to | `src/client/model-choice.ts` — `modelOptions` (catalogue + a stored value it doesn't list, so nothing is silently dropped), `preferredModel` (small model per family: haiku / mini), `correctedModel` (fills an empty one from live-or-fallback; overrules a real choice only against a **live** catalogue — **except another vendor's model, which goes with no catalogue at all**, since Claude Code publishes none and a GPT model on a Claude subscription needs no lookup to rule out — NEWS-278). `VENDOR` pairs `claude-cli`/`anthropic` and `codex-cli`/`openai`; the cross-vendor rule skips endpoint-configurable providers and models no catalogue names. Applied on provider change and Source-tab open. NEWS-253, FR-6.15 |
| Model suggestions in Settings | **Live from the provider** (NEWS-248): `GET /api/models` → `CheckRunner.listModels()` → `provider.listModels?()`; ranking in `src/ai/model-list.ts` (`rankModels` — sorts on the vendor's `created` field, drops non-text families by *exception* and `-YYYY-MM-DD` snapshots). Anthropic reads `client.models.list()` (`src/ai/providers/anthropic-models.ts`, NEWS-251 — RFC 3339 `created_at` converted to epoch; `capabilities.effort` answers the per-model narrowing). Codex reads `~/.codex/models_cache.json` instead (`src/ai/providers/codex-models.ts`, NEWS-249) — the CLI's own catalogue, `visibility`/`priority` honoured. Claude CLI keeps aliases (no local catalogue exists). `PROVIDER_MODELS` in `src/ai/types.ts` is now only the **fallback**. Fetched when the Source tab opens, never on the poll. See `docs/6-providers.md` FR-6.14 |
| Which effort levels to offer | `EFFORT_LEVELS` is the cross-provider **superset**; `PROVIDER_EFFORT_LEVELS` + `provider.effortLevelsFor(model)` decide the menu (NEWS-250). Codex answers per model from `~/.codex/models_cache.json` (`readCodexEfforts`); the rest are vendor-declared sets. `/api/models` returns them; `src/client/effort-options.ts` renders. Each request builder **drops** a level its API doesn't declare. See `docs/6-providers.md` FR-6.13a |
| AI effort / how hard the model works | `EFFORT_LEVELS`/`EFFORT_LABELS`/`Effort` in `src/ai/types.ts`; `EFFORT_PROVIDERS`/`providerTakesEffort` is the single list — **everything except `mock`** (NEWS-239/244/245); `auto` qualifies only because `AUTO_ORDER` ⊆ the list, pinned by `effort.test.ts`. Wire-up per provider: `output_config.effort` (`anthropic.ts`), `--effort` (`claude-cli.ts`), `-c model_reasoning_effort=` (`codex-cli.ts`), `reasoning: { effort }` (`openai.ts`, with `looksLikeEffortRejection` + one-shot retry, since only reasoning models take it). **Checks only** everywhere — Haiku 4.5 rejects it, so discovery must never inherit it. `<select data-action=effort>` in the Source block. `--effort` / `NEWSMONGER_EFFORT`. FR-6.13 | Per-run recording: `runs.effort` column (SCHEMA_VERSION 4), read off `provider.effort` not settings; `null` = not recorded vs `''` = model default. NEWS-226
| `--help` / `--version` | `earlyExitFlag` + `HELP_TEXT`/`USAGE_LINE` in `src/config.ts`, answered at the top of `main()` in `cli.ts` before the store opens. Both go to stdout and exit 0 (NEWS-216) |
| What the published npm package ships / whether a global install works | `package.json` `files`/`bin`/`prepublishOnly` + `tests/unit/npm-package.test.ts`; asset lookup is `clientDir()` in `server.ts` (module-relative, not cwd). Pack-install-run procedure in `docs/manual-test-plan.md` |
| A colour that fails contrast |  — parses the token blocks out of  and checks 4.5:1 for every text pair in both palettes. Exists because axe only sees elements that are *rendering*:  shows once per unavailable provider, so a signed-in CLI hid a real failure locally while CI caught it (NEWS-346). See  FR-3.78 |
| A colour that fails contrast | `tests/unit/color-contrast.test.ts` — parses the token blocks out of `styles.scss` and checks 4.5:1 for every text pair in both palettes. Exists because axe only sees elements that are *rendering*: `.state.warn` shows once per unavailable provider, so a signed-in CLI hid a real failure locally while CI caught it (NEWS-346). See `docs/3-ui.md` FR-3.78 |
| Tauri shell | `src-tauri/src/lib.rs` (`running at ` marker must match `src/cli.ts`) |
| Getting a set-aside database back | `src/recover.ts` (`listSetAside`/`recoverSetAside`) → `GET /api/recover/candidates`, `POST /api/recover`; Settings → Data → Recovery in `client/settings.tsx`. Reads the **directory**, not the FR-4.17 notice (that is dismissible). Never swaps files — reads the copy out and `Store.replaceAll`s it in. See `docs/33-recovery.md` (NEWS-342) |
| "My topics are gone" with the app running fine | The `quarantine` row in `meta` — written by `Store`'s constructor when FR-4.9 sets a database aside, exposed on `/api/state`, cleared by `POST /api/quarantine/dismiss`. Banner is the first child of `#banners` in `client/app.tsx`. See `docs/4-cli-server-storage.md` FR-4.17–4.18 (NEWS-340) |
| Broken/missing story images | `pruneImageCache` in `src/images/cache.ts` — **an empty mark set never sweeps a populated cache** (NEWS-341: a quarantined database made the startup prune delete every image in the install). Repair on demand: `GET /api/image/:hash` → `Store.imageSourceUrl` → `cacheImageUrl`, injected as `refetchImage` on `createApp`. Content-addressed by source URL, so a refetch lands at the same hash and needs no DB write. See `docs/8-article-images.md` FR-8.19–8.20 |
| A window stuck on the loading spinner / a server that won't start | `spawn_server` pipes **stderr** (never `Stdio::inherit`) and `startup_failure_detail()` in `src-tauri/src/lib.rs`; painted by `window.showExited(detail)` in `src-tauri/loading/index.html`. Shown **verbatim** — the server is the only thing that knows why. Tests: the Rust `mod tests` for the formatting, `tests/unit/startup-failure.test.ts` for the contract between the two halves. See `docs/32-startup-failure.md` (NEWS-338) |
| Rust-only CI jobs failing on missing bundle paths | `scripts/ensure-sidecar-stub.sh` — `tauri-build` validates `externalBin`/`resources` in its build script, so plain `cargo clippy` fails before compiling. Used by `ci.yml` and `release-candidate.yml`; stubs only, idempotent (NEWS-191/201) |
| Release bundling / sidecar | `scripts/build-sidecar.sh` + `src-tauri/tauri.conf.json` (`externalBin`, `resources`) |
| A new runtime dependency | just add it to `package.json` `dependencies` — tsup externalizes it and the sidecar script installs it; no list to update |
| Mock behavior in tests | `src/ai/providers/mock.ts` (topic name containing "fail"/"empty" triggers those paths) |
| API keys / keychain | `src/keychain.ts` (OS layer) + `src/ai/api-keys.ts` (env→keychain precedence); `NEWSMONGER_FAKE_KEYCHAIN=1` for tests |
| Foreground/attendance gate | `src/attendance.ts` + `CheckRunner.checkDue`; provider opts in via `attended: true`. **Manual checks (`{manual:true}`) record attendance** so a long sweep isn't deferred (NEWS-44) |
| Article images / SSRF guards | `src/images/` — `safety.ts` (URL vetting), `ogimage.ts` (extract), `cache.ts` (fetch+store+`pruneImageCache`), route `/api/image/:hash` is **cache-only** |
| Image cache pruning | `pruneImageCache`/`liveImageHashes` in `src/images/cache.ts`; called at startup (`cli.ts`) + on `DELETE /api/topics/:id` |
| Settings dialog | **`src/client/settings.tsx`** (NEWS-297) — tabs, all four panels, diagnostics; `keyRowJsx` is in `key-row.tsx` because onboarding renders it too. Routes in `src/routes/api.ts` under `/api/keys`. The `delegate()`s stay in `app.tsx` |
| The Source tab's "is this usable" line | `sourceStatus` in `src/client/source-status.ts` (pure) → `sourceStatusJsx` in `settings.tsx`; `.source-status` in `styles.scss`. **`auto` is resolved client-side** by walking `AUTO_ORDER` over `/api/providers`' probes — the server reports `auto` as `available: null` on purpose, and the line rendered blank for it until NEWS-308. Mirrors `resolveProvider`'s rule deliberately. See `docs/3-ui.md` FR-3.1a |
| Sidebar collapse | `sidebarCollapsed` in `src/client/stores.ts` (localStorage `news:sidebar-collapsed`); `.shell.sidebar-collapsed` in `styles.scss` |
| Topic selection / context menu / solo | `contextMenuJsx` in `client/topics-view.tsx` (NEWS-297); `selectTopic`/`runTopicAction` in `app.tsx`; state in `stores.ts`. **Row state outside the topic object needs `each()`'s cacheKey** |
| Confirm a destructive action | `confirm()` helper + `confirmDialogJsx` in `app.tsx`. **Never `window.confirm` — a silent no-op in Tauri's WKWebView**. Three button weights: `.btn` neutral → `.btn.danger` marked (opens a confirm) → `.btn.danger-solid` filled (the confirm's own final action). `tests/unit/button-variants.test.ts` fails a `btn <variant>` the stylesheet never styles — `.btn.danger` was applied for a release without existing (NEWS-304) |
| Save/bookmark or share a story | `src/client/app.tsx` (bookmark + share buttons in `itemJsx`, `showToast`); `setItemSaved` in `store.ts` + `PATCH /api/items/:id`; `src/client/share.ts`. See `docs/11-story-actions.md` |
| Expanding a story card / the detail pane | `expandedItemId` + `toggleItemExpanded`/`collapseItem`/`collapseExpandedItem` in `client/stores.ts`; the `[data-item-id]` **click** delegate in `app.tsx` (one handler for the body *and* the `[data-expand-item]` button, bailing inside `.item-actions` / `ul.sources` / `.item-pane` and on a live text selection); the always-present `div.item-pane` + `aria-expanded`/`aria-controls` in `itemJsx`; `.item-pane`/`.item.expanded` in `styles.scss`. Accordion (one at a time), ephemeral, collapsed by Escape and by every view change. Flagged one-liners and review-mode cards render **no** expander and stay inert. The pane's *content* is the thread timeline — next row. See `docs/3-ui.md` FR-3.63–3.66 |
| Feed filters (Solo / Saved / Search) | **Server-side** now (NEWS-76): `Store.queryItems` filters; client sends view params via `refreshFeed` (`src/client/api.ts`). Search is debounced. `feedItems`/`feedTotal` in `stores.ts`. See `docs/14-search.md` + `docs/17-server-pagination.md` |
| Feed pagination ("Show more") | `feedLimit`/`FEED_PAGE`/`showMoreFeed` in `stores.ts` (reset per view) → `/api/items?limit=`; `moreCount` from server `total`. See `docs/16-pagination.md` |
| The feed data / where items come from | `/api/items` via `refreshFeed`, NOT `/api/state` (slimmed). Just-flagged overlay: `recentlyFlaggedItems` merged in `app.tsx`. **Per-story client state a server refresh cannot reach** — that overlay, `reviewTopicIds`, `expandedItemId`/`threadShowAll`, `threadPanes` — must be dropped explicitly when stories are deleted: `clearStoryOverlays()` for the app-wide clear (NEWS-291), `clearStoryOverlaysForTopic(id)` for the per-topic one (NEWS-303 — scoped, so it does not throw away other topics' overlays), both called **before** the request. Otherwise the UI keeps describing rows that are gone. **Add a per-story field → add it to both.** Notifications read `latestItemIds`. See `docs/17-server-pagination.md` |
| Verifying a *published* macOS release | `bash scripts/verify-released-dmg.sh <tag> [aarch64\|x64]` — downloads the real dmg, sets `com.apple.quarantine` as a browser would, and checks stapling, `spctl` = `Notarized Developer ID`, quarantine inheritance through drag-out, and that the **sidecar starts and JITs** under the hardened runtime. Distinct from `verify-signing.sh`, which checks a bundle on the machine that built it (holds the key, never quarantines its own output). NEWS-21 |
| Verifying the sidecar on Linux | `bash scripts/verify-sidecar-linux.sh [triple]` — runs `build-sidecar.sh` in a Linux container so the isolated **boot check actually runs** (it self-skips when cross-compiling). Both Linux triples pass (NEWS-20). Does *not* cover the Tauri bundle or launching the app — those need a VM |
| README images (hero + screenshots) | `scripts/demo/capture-demo.ts` (animated hero → `assets/demo.svg`, `npm run demo:capture`) and `scripts/demo/capture-stills.ts` (seven scenes → `assets/stills/*.{png,svg}`, `npm run demo:stills`); shared chrome in `scripts/demo/chrome.ts`; fixtures in `src/demo.ts`; drift guard in `tests/unit/stills.test.ts`. Both drive a real `--demo` server and must run **outside the sandbox**. `--demo` also swaps in `demoProbeProviders` / `demoKeysResponse` (`src/ai/providers/demo.ts`) as `createApp`'s `probe` / `demoKeys`, so `/api/providers` and `/api/keys` answer from a fixture instead of the capturing machine — NEWS-315, guarded by `tests/unit/demo-determinism.test.ts`. See `docs/28-demo-capture.md` |
| Clearing every story (keeping topics) | `Store.clearAllItems()` (one transaction, applies `RESET_CHECK_STATE_SQL`) → `POST /api/items/clear` (cancels checks first, NEWS-271) → the `clear-stories` control in Settings → Data, in its own **Reset** group at the foot of the tab and carrying `btn danger` (NEWS-304 — it shipped inside `Backup`, styled as a twin of `Back up now`). No undo — `ClearUndoBuffer` holds 8, so a bulk undo would be partial. NEWS-255, FR-27.11 |
| What a clear resets (and why it isn't then due) | `RESET_CHECK_STATE_SQL` in `src/db/store.ts` — the field-by-field audit lives in its doc comment; used by **both** `clearItemsForTopic` and `clearAllItems`. Due-ness comes from `scheduleBaseline(topic)` = `lastCheckedAt ?? clearedAt` in `src/checks.ts`, read by `isDue`/`isDueDaily`/`byCheckOrder`. **Display asks `lastCheckedAt`, scheduling asks the baseline** — that split is what lets a cleared topic read "not checked yet" without starting a sweep on the next tick. `items` **is** the dedup ledger (`dedupeKeysForTopic` reads it), so there is no separate "seen" store to reset. Run history survives on purpose. NEWS-291, FR-1.15 + FR-2.13 |
| Restoring from a backup | `readBackup`/`restoreBackup` in `src/backup.ts` → `Store.replaceAll` (one transaction, deletes first, keeps this machine's `backupDir`); `GET /api/backup/preview` + `POST /api/backup/restore` (409 while a check runs); Settings → Data panel + confirm in `app.tsx`. Safety copy to `pre-restore-*.json` in the **data dir**. See `docs/27-data-location.md` FR-27.10 (the *restore workflow*; the path-normalization requirement that used to share this id is FR-27.12 — NEWS-302) |
| The backup offer after the 3rd topic | `src/client/backup-prompt.ts` (pure decision + snooze), `backupOfferJsx` in `client/dialogs.tsx` (NEWS-297) + the `backup-offer-*` delegates in `app.tsx`, `backupPromptNever`/`backupPromptSnoozedUntil` settings, `src/backup-locations.ts` for the suggestions. **No backdrop dismiss, no Escape** (FR-27.3) — the only dialog like that. See `docs/27-data-location.md` |
| Backups to a synced folder | `src/backup.ts` (`Backups`, `writeBackup`, `MIN_BACKUP_INTERVAL_MS`); `backupDir` in `SettingsSchema`; wired in `src/cli.ts` (startup) and `CheckRunner` opts (after a successful check); `POST /api/backup` for "Back up now"; Settings → Data UI. Format is `DataFileSchema`, so the legacy `data.json` import (FR-4.8a) *is* the restore path. See `docs/27-data-location.md` |
| Export / the Atom feed | `src/export.ts` (pure renderers) + `/api/export.md`, `/api/export.json`, `/feed.xml` in `routes/api.ts`; Settings "Export & feed" block. `scope=all\|saved\|topic`. **The feed needs an `atom:author` (RFC 4287 4.1.1) and `escapeXml` must strip the control characters XML cannot represent** — one of either makes a reader reject the whole feed, and it can only say "it doesn't work" (NEWS-330). See `docs/21-export-and-feed.md` |
| Sharing a **topic list** | `topicsToJson` in `src/export.ts` + `/api/export-topics.json`; `Store.importTopics` + `/api/import-topics`; the `Topics` group in Settings → Data, export as `<a download>` and import as a `<label class="btn file-btn">` wrapping a clipped file input; both pairs share `.io-row` (NEWS-327). Carries name/guidance/category only — **the exclusions are the requirement**, so `paused`, ids and every timestamp are asserted absent. Import is **additive, idempotent and never merges**; `Store.topicExists` is the single duplicate rule `addTopic` and `importTopics` share, so the form and the file cannot disagree. A different thing from a backup, which is the personal-transfer format. See `docs/30-import.md` FR-30.2–30.9 (NEWS-317/318) |
| Reading an **exported archive** back | `Store.importStories` + `/api/import-stories`; *Import stories…* beside *Export stories…* in Settings → Data. Dedups with `filterNewItems` — the same function a check uses — so import and checking agree on "the same story"; **creates the topic** a story names if it is missing (the export carries a name, not an id); preserves `foundAt` and `saved`; calls `backfillThreads` so imported stories thread in. `items` **is** the dedup ledger, so an import narrows what later checks report — pinned by a sequence test. See `docs/30-import.md` FR-30.10–30.14 (NEWS-319) |
| Story retention / data-file growth | `Store.pruneOldItems` + `itemRetentionDays` setting; called from `cli.ts` at startup and `CheckRunner.pruneAfterCheck` after each success. Bookmarked + flagged items exempt. See `docs/4-cli-server-storage.md` FR-4.11 |
| Diagnostics / "why did a check fail" | **Gone since NEWS-333** — `src/client/diagnostics.ts` and the Settings section are deleted, and with them the app's only bug-report path. A failed check now surfaces through the banner alone (`.banner.warn` in `client/app.tsx`), and its verbatim error text is not reachable from the UI. Run history is still stored and still drives that banner (FR-25.7). See `docs/3-ui.md` FR-3.25–3.28, all four now *Removed* |
| Solo filter (set arithmetic, menu + double-click) | `src/client/solo.ts` (`toggleSolo`, `isAllSoloed`), used by `runTopicAction` and the `dblclick` delegate in `app.tsx`. See `docs/3-ui.md` FR-3.40 |
| Topic categories / taxonomy | `src/categories.ts` (`BUILTIN_CATEGORIES`, `categoryLabel`, `activeCategories`). See `docs/22-topic-categories.md` |
| Topic discovery / suggestions | `NewsService.suggestTopics` + `SuggestRequest`/`SuggestScope`/`TopicSuggestion` in `src/ai/types.ts`; prompting + parsing in `src/ai/suggest-prompt.ts`; all 5 providers (NEWS-124). Server: `src/discovery.ts` (`DiscoveryService` — exclusions, request cache, classification validation, call log) + `POST /api/discover` / `GET /api/discover/usage` (NEWS-125). Client: `src/client/discover.ts` (grouping/headings) + the dialog in `client/discover-view.tsx` (NEWS-126, moved NEWS-297). Tuner (NEWS-127) + onboarding integration (NEWS-128) shipped. Variation D (newsstand) deferred — NEWS-129. See `docs/24-topic-discovery.md` |
| Provider retries / rate limiting | `src/ai/retry.ts` (`backoffDelayMs`, `classifyFailure`, `retryAfterMs`, `DEFAULT_BACKOFF` vs `FAILURE_COOLDOWN`); `checkWithRetry` + `rateLimitedUntil` in `checks.ts`. See `docs/23-retries-and-rate-limits.md` |
| A topic held back after failures | `consecutiveFailures`/`retryAfter` columns; `recordCheckFailure`/`clearCheckFailures` in `db/store.ts`; the cooldown check at the top of `isDueUnderSchedule`. See FR-23.7 |
| Settings tabs / panels | `SETTINGS_TABS`, `settingsTabsJsx`, `settingsPanelJsx` in `client/settings.tsx` (NEWS-297); `settingsTab` in `stores.ts`; `.settings-tabs` in `styles.scss`. See `docs/3-ui.md` FR-3.45 |
| Privacy dialog | `privacyDialogJsx`/`privacyNoteJsx` in `client/dialogs.tsx` (NEWS-297) + `#privacy-slot` + the footer `[data-action=open-privacy]` in `app.tsx`. See FR-3.47 |
| Source favicons in the feed | `src/images/favicon.ts` (origin canonicalisation + `<link rel=icon>` extraction) → `cacheFavicon` in `images/cache.ts` → `NewsSource.favicon` → the `img.favicon` / arrow-fallback branch in `client/app.tsx`. **Keyed per ORIGIN, not per article** — one outlet cited six times is one request. Resolved in `CheckRunner.resolveFavicons` (deduped across the whole batch), passed via the constructor's `opts.fetchFavicon`. **Favicons must be in `liveImageHashes` or the startup prune silently deletes them.** See `docs/8-article-images.md` FR-8.14–8.18 |
| Window titlebar text (macOS) | `hiddenTitle: true` in `src-tauri/tauri.conf.json`'s window config. **`title` must stay set** — the Window menu and Dock context menu read it; `title: ""` hides the text and breaks both. macOS-only; Windows/Linux keep theirs by design. See `docs/5-desktop-app.md` FR-5.10 |
| Desktop app *name* (Dock hover/menu bar) | `[[bin]] name` in `src-tauri/Cargo.toml` — macOS reads **`CFBundleExecutable`** for a running app, *not* `CFBundleName`. Renamed in Cargo rather than `mainBinaryName` so `tauri dev` is fixed too. Gated by `tests/unit/tauri-naming.test.ts`. See `docs/5-desktop-app.md` FR-5.9 |
| Desktop app icon (Dock/Finder/About) | `bundle.icon` in `src-tauri/tauri.conf.json` — **the schema default is `[]`, so omitting it ships an iconless app**. Assets in `src-tauri/icons/` from `npx tauri icon`. Gated by `tests/unit/tauri-icons.test.ts`. See `docs/5-desktop-app.md` FR-5.8 |
| Desktop auto-update | **Rust**: `PendingUpdate` + `get_pending_update`/`check_for_update`/`install_update` in `src-tauri/src/lib.rs` (all `#[cfg(not(debug_assertions))]`-guarded), `plugins.updater` + `createUpdaterArtifacts` in `tauri.conf.json`, `updater:default`/`process:default` in `capabilities/default.json`. **Client**: `pollPendingUpdate`/`requestUpdateCheck`/`installPendingUpdate` + the `.banner.update` block + `install-update`/`dismiss-update`/`check-updates` delegates in `client/app.tsx`; `updateVersion`/`updateDismissed`/`updateInstall`/`updateChecking`/`updateCheckMessage` in `stores.ts`; `getTauriInvoke` in `client/tauri.ts`; `.banner.update`/`.update-check` in `styles.scss`. Manifest is `latest.json` on `releases/latest`, published by `release-desktop.yml`. Private key `~/.tauri/newsmonger.key` — **losing it strands every installed build**. Tests: `tests/unit/update.test.ts`, `tests/e2e/update.spec.ts`. See `docs/5-desktop-app.md` FR-5.12–5.17 |
| Signed build on your own machine | `npm run tauri:build:local` → `scripts/tauri-build-local.sh` (NEWS-198). Resolves `APPLE_SIGNING_IDENTITY` from the login keychain **by organization** (`Small Tale`) and derives `APPLE_TEAM_ID` from it — this machine has five Developer ID certs, four personal. `read -rs` for the password (never in argv/history/`ps`), `--no-notarize` for speed, `--check` to validate setup in a second, then runs `verify-signing.sh`. Supplies the updater key, which `createUpdaterArtifacts` now requires (NEWS-89). **Not a substitute for CI** — local signs from the login keychain, CI imports a `.p12`. **Scripts must be bash 3.2-clean** (macOS default); gated by `tests/unit/release-scripts.test.ts` |
| Cutting a release (the scripts) | `npm run release` → `scripts/release.sh` tags **`v{ver}-rc.N`** (CI promotes; the clean `v{ver}` tag is created by CI, never by you); `release:beta` → same flow, `v{ver}-beta.N`; `release:beta:auto` → `scripts/release-beta-auto.sh` (non-interactive, `--dry-run`). Interactive flow is resumable via `.release-state.json`. Notes drafted by **gitgist** over `<lastVersionTag>..HEAD` using `--match 'v*'` (this repo has non-version tags). **`scripts/set-version.mjs`** writes the three files `npm version` misses — `tauri.conf.json`, `Cargo.toml`, **`Cargo.lock`** — and **accepts `X.Y.Z[-prerelease]`** (rejecting only build metadata). It used to reject prerelease suffixes on NEWS-196's inherited claim that macOS bundle fields do; **measured false in NEWS-207** — a local `0.2.0-beta.1` build bundles and the launched app reports that version — and the base-version workaround meant every beta bundle reported the same version, so the updater could never order two betas. In the tree the files carry the clean `X.Y.Z`; CI writes the full `-beta.N` at build time. The real Windows-MSI constraint is handled with `--bundles nsis`. Two of the four glassbox divergences are gone as of NEWS-201; the header of `release.sh` is the record. Gated by `tests/unit/release-scripts.test.ts`. NEWS-194 |
| Release pipeline (two workflows, split by tag shape) | **`release-candidate.yml`** on `v*-rc.*` + `v*-beta.*` (NEWS-201, ported from glassbox): gates/audit/rust/e2e/npm-pack/4-target Tauri → npm `@beta` (OIDC trusted publishing, **no `NPM_TOKEN`**) → smoke → then **rc**: promote to `@latest`, create `v{ver}`, dispatch the desktop build; **beta**: signed bundles into a draft prerelease, then flip. **`release-desktop.yml`** on `v[0-9]*` (rc/beta excluded): draft → 4-target signed build → rename assets → flip `draft:false`. **Do not rename `release-candidate.yml`** — npm's trusted-publisher binding is filename-scoped and breaking it surfaces as a registry **404**. Needs `npm install -g npm@11` (OIDC floor is 11.5.1) and an `npm-publish` GitHub environment. The `if: contains(github.ref, '-rc.'/'-beta.')` conditions are the design — see `docs/5-desktop-app.md` → "Cutting a release". **Proven end to end on `v0.2.0-beta.7`** (all four targets signed, macOS notarized in 8 min). Two safeguards are pinned *per job* in `tests/unit/release-scripts.test.ts` (NEWS-234): the signing job's `timeout-minutes` + notary-watch wiring, and **no job another job `needs` may be `continue-on-error`** — such a job reports success even when its steps fail |
| Notarization going quiet in CI | `scripts/notary-watch.sh` — `start` before the build, `report` on `always()`, `history` on `failure()`; wired into the macOS shards of both release workflows with `timeout-minutes: 120`. `In Progress` vs `Invalid` is the whole diagnosis, and `history` prints the submission id + `notarytool log`. Silent no-op without `APPLE_*`. NEWS-197 |
| Whether a signed bundle is actually distributable | `scripts/verify-signing.sh` (FR-5.7) — Developer ID authority, hardened runtime, sidecar JIT entitlements, absent `get-task-allow`, deep strict verify, stapled ticket on **app and .dmg**. Runs on the macOS shards of both release workflows and **fails the job** (NEWS-220). Discovers the bundle in both the plain and `<triple>/` layouts |
| Tag vs version files disagreeing | `scripts/check-tag-version.sh` — compares the tag's **base** against `package.json` + `tauri.conf.json`. Own `version-guard` job in `release-candidate.yml` (every expensive job needs it); first step of `create-release` in `release-desktop.yml`. Exit 1 = mismatch, 2 = no tag given. NEWS-208 |
| macOS asking for Documents/Downloads/Music access | `agentCwd()` in `src/ai/providers/agent-cwd.ts` — spawned `claude`/`codex` get an explicit neutral cwd, and `src-tauri/src/lib.rs` anchors the release sidecar. TCC attributes a child's file access to the responsible app, so an inherited cwd became "Newsmonger would like to access your Documents". NEWS-219 |
| Verifying a *bundle* installs and launches | Windows: manual, `prlctl` into Parallels (`docs/manual-test-plan.md`). Linux: `.github/workflows/linux-bundle-diagnostic.yml` — dispatch-only, installs a published `.deb`/`.AppImage` on a runner under Xvfb, asserts sidecar + served assets + a non-blank window + no orphan. macOS: local. See `docs/5-desktop-app.md` FR-5.3 |
| Published-install smoke test | `tests/smoke/smoke-test.sh` (NEWS-201), run by CI against a globally-installed published package, fresh and after upgrade from `@latest`. Answers what unit/E2E structurally cannot: they run against the working tree, where every file exists regardless of the `files` allowlist. **Scrapes `/static/…` refs out of the served HTML + client bundle** rather than hardcoding a list (a hardcoded list is how the wordmark shipped broken), creates a topic and asserts a `--ai-test` check produces stories, and uses `--data-dir` into `mktemp` — never `~/.newsmonger`. **bash 3.2-clean** |
| Release credentials (certs, secrets, GitHub Actions names) | `docs/5-desktop-app.md` → "What still needs a human — the full credential recipe" and "GitHub Actions secrets". Generation steps, the exact Tauri v2 env var names, the secret table, and production-vs-beta. Notarization uses an **app-specific password** (`APPLE_ID` + `APPLE_PASSWORD` + `APPLE_TEAM_ID`), so all **seven** secrets are plain strings mapping 1:1 onto Tauri env vars and the release job needs no credential-prep step. **Nothing is set up yet** — no signing identity on the dev machine, and `.github/workflows/ci.yml` is gates only (no release job). Traps documented there: `APPLE_PASSWORD` is the app-specific password (with hyphens), not the Apple ID password; changing the Apple ID password **revokes** it; the release runner must be macOS. The App Store Connect key route is the documented migration target and reintroduces the `APPLE_API_KEY_PATH` file-materialization step. See NEWS-21 |
| An edit to `CLAUDE.md` that vanished | It was inside a `hotsheet:begin section=` block — shared template text a sync rewrites. Project-specific guidance goes in a nested `specifics=` block or outside the markers entirely; see CLAUDE.md → "Editing this file". `tests/unit/claude-md.test.ts` checks the markers pair up and no heading is duplicated (an orphaned `end` had the section in the file twice). NEWS-310 |
| Agent skills (`/design-review`, `/hotsheet`, kerf) | `.claude/skills/<name>/SKILL.md`, auto-discovered by Claude Code. `design-review` (NEWS-262) is a design-director critique adapted from an upstream `critique` skill — its preparation reads `docs/3-ui.md`, captures the UI with `npm run demo:stills`, and files findings as Hot Sheet tickets. Every repo path and npm script a skill names is held to the tree by `tests/unit/skill-references.test.ts` |
| Project ownership / repo metadata | `package.json` (`author`, `repository`, `bugs`, `homepage`) + `identifier` in `src-tauri/tauri.conf.json` — owner is **Small Tale Inc.**, repo is **[Small-Tale/newsmonger](https://github.com/Small-Tale/newsmonger)**, bundle prefix is `com.smalltale`. Gated by `tests/unit/ownership.test.ts`. The `brianwestphal/kerf` links in `README.md`/`CLAUDE.md`/`.claude/skills/kerf-app/SKILL.md` are **third-party** (kerfjs still lives there) — do not rewrite them. See `docs/5-desktop-app.md` FR-5.11 |
| Icons, favicon, web app manifest | `assets/*.svg` sources; `manifest()` + the `/manifest.webmanifest` route in `src/routes/pages.tsx`; `<link>`s in `src/components/layout.tsx`. See `docs/3-ui.md` FR-3.43–3.44 |
| Masthead wordmark | `assets/wordmark-{light,dark}.svg` → copied to `dist/client` by the `build:client`/`build:client:dev` copy list in `package.json` → served from `/static/`. `<picture>` + `prefers-color-scheme` in the `<h1 class="wordmark">` in `client/app.tsx`; `.wordmark` in `styles.scss`. See `docs/3-ui.md` FR-3.58 |
| Adding a client static asset | Edit the copy list in **both** `build:client` and `build:client:dev` — and only those. `scripts/build-sidecar.sh` copies everything `dist/client/` contains (minus `*.map`), so the Tauri bundle needs no separate edit. It used to name files, which made it an invisible **third** list and shipped a broken masthead in the packaged app (NEWS-203). Gated by `tests/unit/client-assets.test.ts`, which checks every `/static/…` the client references is copied, that the two lists agree, that the file exists in `assets/`, and that neither hardcoded list comes back |
| Brand colours in an asset vs the stylesheet | `tests/unit/brand-assets.test.ts` — parses the `:root` / dark-`:root` token blocks out of `styles.scss` and the fills out of both wordmark SVGs, then asserts each mark's accent **is** its theme's `--pine` and clears a contrast floor on its `--paper`. Edit a token or re-export a mark and this is the gate that catches the drift. See `docs/3-ui.md` FR-3.58a |
| Which filter pills are shown | `visibleCategories`/`visibleSubcategories`/`hasUncategorized` in `src/categories.ts` (pure, over the topic list). See `docs/22-topic-categories.md` FR-22.13–22.15 |
| Renaming a topic / clearing its stories | `Store.renameTopic` / `clearItemsForTopic`; the story count comes from `GET /api/items?topics=<id>&limit=1` when the dialog opens (**not** `/api/state`, which is polled every 4 s); `PATCH /api/topics/:id { name, clearItems }`; `renameDialogJsx` in `client/dialogs.tsx` (NEWS-297), `saveRename` + the delegates in `client/app.tsx`. See `docs/25-topic-editing.md` |
| Sidebar sort order / section headings | `src/client/topic-sort.ts` (`sortTopics`, `topicRows`); `TOPIC_SORTS`/`TOPIC_SORT_LABELS` in `client/stores.ts`; `.topic-section` in `styles.scss`. See `docs/3-ui.md` FR-3.2a |
| Section filter bar / sidebar pills | `filterBarJsx` in `client/filter-bar.tsx` (NEWS-297) + the `[data-filter-category]`/`[data-filter-subcategory]` delegates in `client/app.tsx`; `.filter-bar` in `styles.scss`; server filter in `Store.queryItems`. See `docs/22-topic-categories.md` FR-22.9–22.12 |
| Automatic topic classification | `needsClassifying`/`classifierOptions`/`applyClassification` in `checks.ts`; prompt + parsing in `ai/prompt.ts`. See `docs/22-topic-categories.md` FR-22.8 |
| Stale refresh overwriting newer state | sequence guards in `refreshState`/`refreshFeed` (`src/client/api.ts`). See `docs/17-server-pagination.md` FR-17.9 |
| **When** the client asks for state (poll cadence, hidden pages, catch-up) | `src/client/poll.ts` — extracted from `app.tsx` so the policy is unit-testable at all (`tests/unit/poll.test.ts`). Deps are injected; `browserPollDeps` is the real wiring. Its sibling is the *ordering* question above: NEWS-104 pinned which answer wins, this pins when we ask. NEWS-238 |
| A `<select>` not following a server-side change | `src/client/select-sync.ts` — after every render, the option carrying the `selected` **attribute** wins over the live `value`. The HTML dirtiness flag stops a touched control following the attribute, and kerf can only re-sync the property when that attribute *changes*. Hooked from an `effect` + `queueMicrotask` after `mount` in `app.tsx`. NEWS-238 |
| Whether a CI job can block a release | `needs:` + absence of `continue-on-error` in `.github/workflows/release-candidate.yml`, pinned by `tests/unit/release-scripts.test.ts`. `test-e2e-windows` is **blocking** as of NEWS-235 — a job another job `needs` must never be `continue-on-error`, since it would go green with failing steps |
| Socket churn / `ERR_NO_BUFFER_SPACE` on a Windows runner | Shared browser context in `tests/e2e/fixtures.ts` + `KEEP_ALIVE_TIMEOUT_MS` in `src/server.ts` (Node's 5 s default sat 1 s above the client's 4 s poll). Measure with `netstat -an -p tcp \| grep "\.$(the derived E2E port) "`. NEWS-246 |
| The E2E port / two checkouts colliding on it | `tests/helpers/e2e-port.ts` — SHA-256 of the checkout path → a 2-port window in 4200–4999 (`E2E_SERVER`, `E2E_REAL_SERVER`). Used by `playwright.config.ts` and `tests/e2e/real-providers.spec.ts`; **nothing may hardcode a port**, pinned by `tests/unit/e2e-port.test.ts`. `scripts/e2e-preflight.mjs` runs at config load (guarded to the main process — workers re-import the config) and reclaims an orphaned server or refuses a live one. NEWS-287 |
| Why the Rust gates did or did not run | `scripts/rust-changed.sh` prints its reasoning — uncommitted work, then commits not yet on the upstream. Overrides: `RUST_GATES=required` (always run), `RUST_GATES=skip` (never), `CI` set (auto-skip disabled). Pinned by `tests/unit/rust-changed.test.ts`, which drives the real scripts against a stubbed `git`/`cargo` because a wrong skip produces a **green** run, not a red one. NEWS-294 |
| Recording that a requirement shipped | Its marker in `docs/N-*.md` — `*(Shipped)*` / `*(Partial)*` / `*(Design only)*` / `*(Deferred)*` / `*(Rejected)*` / `*(Decided)*` / `*(Removed)*`, immediately after the bold id — then `npm run docs:requirements`. `requirements-summary.md`'s status block is generated from those markers and its notes region is not; `scripts/build-requirements-summary.mjs` **errors on a marker it cannot read** rather than emitting "unknown", and `tests/unit/requirements-summary.test.ts` fails when the committed file is stale. NEWS-296 |
| An E2E failure that may not be real | `tests/helpers/server-alive.ts` — probed from the `page` fixture on every failure; prints "THIS RUN IS VOID" when `/healthz` does not answer, because a dead shared server makes every later assertion in the file meaningless (NEWS-298). Wording pinned by `tests/unit/server-alive.test.ts`; the wiring is a manual check in `docs/manual-test-plan.md`. Failures also carry a `state-dependent` annotation naming the file-level dependency |
| A background sweep interfering with a test | `NEWSMONGER_SCHEDULER_TICK_MS` (`schedulerTickMs` in `src/scheduler.ts`), set beyond a run's length in `playwright.config.ts`. Every E2E spec shares one server, and a scheduled check writes into the state they assert on. See `docs/4-cli-server-storage.md` FR-4.4a |
| Orphaned stories/runs after a topic delete | `Store.pruneOrphans()` in `db/store.ts`, called from `pruneAfterCheck` (`checks.ts`) and at startup (`cli.ts`). See `docs/4-cli-server-storage.md` FR-4.8c |
| Wide-window layout / column count | `.shell` (no max-width) and `.day` (`auto-fill, minmax(400px, 1fr)`) in `styles.scss`. Guarded by `tests/e2e/layout.spec.ts`. See `docs/3-ui.md` FR-3.36–3.39 |
| Accessibility (keyboard, ARIA, focus) | `trapTabInDialog` + the global `keydown` handler + `openTopicMenuFor` in `app.tsx`; `role=listbox/option` on the topics list; `:focus-visible` rule in `styles.scss`. Guarded by `tests/e2e/a11y.spec.ts`. See `docs/3-ui.md` FR-3.20–3.24 |
| The privacy disclosure | `privacyNoteJsx` in `app.tsx` (Settings → Privacy), README "Privacy", onboarding welcome step; pinned by a test in `tests/unit/guidance.test.ts`. See `docs/7-api-keys.md` FR-7.13 |
| First-run onboarding | `onboarding`/`onboardingTopics`/`STARTER_TOPICS` in `stores.ts`; **markup in `client/onboarding-view.tsx`** (NEWS-297), `maybeOpenOnboarding` + the delegates in `app.tsx`; dismissal in localStorage `news:onboarding-seen`. Key check: `src/ai/verify-key.ts`, injected via `createApp({verifyKey})` (null under `--ai-test`). See `docs/20-onboarding.md` |
| Per-topic guidance (free-text steer) | `guidance` on the topic + `setTopicGuidance` in `store.ts`; `PATCH /api/topics/:id`; `TopicContext` → `buildUserPrompt` in `src/ai/prompt.ts`; menu action `guidance` + `guidanceDialogJsx` in `client/dialogs.tsx` (NEWS-297), `guidanceTopicId` in the store. See `docs/18-topic-guidance.md` |
| Flag a story off-topic / review mode | `itemMenuJsx`/`flaggedRowJsx` in `client/feed.tsx` (NEWS-297); `offTopic` on items + `setItemOffTopic`/`offTopicTitlesForTopic` in `store.ts`; `PATCH /api/items/:id`; item context menu (`itemMenuJsx`), `flaggedRowJsx`, `reviewTopicIds`/`recentlyFlagged` in `app.tsx`; prompt via `buildUserPrompt` offTopicTitles. See `docs/15-off-topic-flagging.md` |
| Transient toast | `#toast-slot`/`.toast` + `showToast` in `app.tsx`; `toast` state in `stores.ts`. **Never `window.alert` — a WKWebView no-op** |
| Who is allowed to call the API | `src/origin-guard.ts`, mounted first in `createApp`. Loopback binding is not a fence against the user's *own* browser — Host + Origin are. Absent `Origin` is allowed on purpose (curl / `app.request`); it is not authentication. See `docs/4-cli-server-storage.md` FR-4.5a |
